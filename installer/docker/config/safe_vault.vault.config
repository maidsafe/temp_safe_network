{
    "wallet_address": %{env:SAFE_VAULT_WALLET_ADDRESS},
    "max_capacity"  : %{env:SAFE_VAULT_MAX_CAPACITY},
    "dev": {
        "disable_mutation_limit": %{env:SAFE_VAULT_DISABLE_MUTATION_LIMIT}
    }
}
